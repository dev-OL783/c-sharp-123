namespace A1Q3;

class Program
{
    static void Main(string[] args)
    {

        // Keep this line intact 
        Console.WriteLine("===========================");

        // prompts the user to enter a cricket score result
        Console.WriteLine("Please enter the over scorecard:");
        // converts results to string
        string scorecard = Console.ReadLine();

        // only accept numbers between 1 and 6 characters [1, 6] 
        // for runs, numbers can be displayed between 1 and 6 representing the number of runs a ball scores 
        // W for wickets, symbolizing a successful hit from the baller
        // . representing no runs were successfully made
        // any other input/character should make the scorecard invalid and cause a invalid message




        // Keep this line intact 
        Console.WriteLine("===========================");
    }
}  